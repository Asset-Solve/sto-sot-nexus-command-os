---
name: transactional-uiux-screens
description: Use when designing or fixing screens so they become guided transactional workbenches instead of dashboards or CRUD pages.
---
# Transactional UI/UX Skill

Every screen must declare persona, process step, source object, target object, fields, lookups, backend endpoints, workflow states, actions, validations, payload preview, approvals, outbox, read-back, reconciliation, audit, and tests.
