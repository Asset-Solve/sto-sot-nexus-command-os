---
name: enterprise-domain-modeling
description: Use when creating capability models, process decomposition, personas, data object classifications, source-of-record rules.
---
# Domain Modeling Skill

Build capability → process → subprocess → task → activity → workflow event → action → data object → integration → audit mapping. Separate master, transaction, reference, derived, audit, reconciliation, AI evidence, and event data.
