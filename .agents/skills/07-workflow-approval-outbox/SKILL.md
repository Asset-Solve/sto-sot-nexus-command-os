---
name: workflow-approval-outbox
description: Use when implementing state machines, approvals, four-eyes, outbox, write-back, read-back, reconciliation, corrections, reversals.
---
# Workflow Approval Outbox Skill

Build state machines and controlled write paths. Every outbound write requires approval as configured, payload preview, outbox, idempotency, correlation ID, retry, DLQ, read-back, reconciliation, audit.
